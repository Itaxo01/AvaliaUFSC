-- =============================================
-- V1: Baseline - Migração vazia
-- =============================================
-- Este é um baseline para o banco de dados existente.
-- As tabelas já foram criadas pelo Hibernate auto-ddl.
-- Esta migração existe apenas para estabelecer um ponto de partida
-- para o Flyway em bancos que já possuem as tabelas.

-- Nada a fazer - as tabelas já existem

